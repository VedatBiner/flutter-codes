name: sozluk_ser_tr_ui
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.8.2+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  firebase_core:
  firebase_auth:
  cloud_firestore:
  google_sign_in:
  google_fonts:
  provider:
  package_info_plus:
  flag:
  firebase_ui_firestore:
  carousel_slider:
  path_provider:
  fluttertoast:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/database/